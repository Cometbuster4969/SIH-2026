"""SatQuery AI — Window A prototype skeleton.

Real where it counts (ingestion, registry, RuleRouter, trace, orchestration,
one genuinely trained RS tool via BIFOLD BigEarthNet v2.0 weights); heuristic
where declared (change mask, grounding on demo scenes). Becomes the Window B
skeleton — trained models slot into the same Tool contract.
"""

__version__ = "0.1.0"
